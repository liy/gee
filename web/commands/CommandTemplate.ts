class CommandSection {
  index: number;
  text = '';
  constructor(index: number) {
    this.index = index;
  }
}

// export default class CommandTemplate {
//   sections: Array<CommandSection>;
//   constructor() {}
// }
